/**
 * @qbite/shared-types
 *
 * Placeholder entry point. This package will hold TypeScript types
 * shared between apps/backend and apps/admin (e.g. Order, Restaurant,
 * User shapes) once a real duplication problem exists between them —
 * per ARCHITECTURE.md §8, it is intentionally not populated
 * speculatively during the engineering-foundation phase.
 */
export {};
