/**
 * @qbite/api-contracts
 *
 * Placeholder entry point. This package will hold the shared source of
 * truth for request/response schemas and error codes described in
 * docs/API_SPECIFICATION.md (e.g. as Zod schemas backend validation and
 * apps/admin's typed API client both import from) — populated starting
 * when the first real endpoint is implemented, not before.
 */
export {};
