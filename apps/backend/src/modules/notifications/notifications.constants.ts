/**
 * Duplicated rather than imported from another module's constants
 * file — same rationale menu.constants.ts already documents: keeps
 * this module independent, and the two numbers are pure config with
 * no correctness risk if they ever drift.
 */
export const DEFAULT_PAGE_SIZE = 20;
export const MAX_PAGE_SIZE = 50;
