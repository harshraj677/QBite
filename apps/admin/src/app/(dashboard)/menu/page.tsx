import { MenuDirectoryPage } from '@/features/menu/components/menu-directory-page';

export const metadata = { title: 'Menu Items' };

export default function MenuPage() {
  return <MenuDirectoryPage />;
}
