import { CanteensDirectoryPage } from '@/features/canteens/components/canteens-directory-page';

export const metadata = { title: 'Canteens' };

export default function CanteensPage() {
  return <CanteensDirectoryPage />;
}
