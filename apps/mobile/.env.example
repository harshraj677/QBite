# QBite Mobile — Environment Variables (example)
# Copy to .env.development / .env.staging / .env.production and fill in real values.
# Never commit actual .env.* files (only .env.example is tracked — see root .gitignore).

API_BASE_URL=https://api.dev.qbite.app/api/v1
SOCKET_URL=wss://api.dev.qbite.app
RAZORPAY_KEY_ID=
GOOGLE_MAPS_API_KEY=
